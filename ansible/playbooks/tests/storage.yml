# ansible/playbooks/tests/storage.yml
---
- name: Test Storage Management
  hosts: localhost
  gather_facts: false

  roles:
    - role: hetzner/storage
      vars:
        storage_action: "{{ action | default('create') }}"