---
# Cloud Provider Configuration
hetzner_token: "your-hetzner-api-token"  # Required: Your Hetzner Cloud API token
hetzner_ssh_key_name: "your-email@example.com"  # Required: Name for your SSH key in Hetzner

# Server Types (see https://www.hetzner.com/cloud for options)
app_server_type: "cax11"  # ARM-based server for applications
gpu_server_type: "ccx63"  # GPU server type

# S3 Configuration
s3_enabled: true  # Set to true to use S3-compatible storage
s3_access_key: "your-s3-access-key"  # Required if s3_enabled=true
s3_secret_key: "your-s3-secret-key"  # Required if s3_enabled=true
s3_bucket: "your-bucket-name"  # Required if s3_enabled=true
s3_endpoint: "https://your-s3-endpoint.com"  # S3-compatible endpoint
s3_region: "eu-north-1"  # Optional, defaults to us-east-1

# Development Settings
debug: false  # Enable debug logging 